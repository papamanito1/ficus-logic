import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'FELIX | Ficus Logic',
  description:
    "FELIX — Ficus E Logic Intelligence Exchange is Ficus Logic's AI recruitment assistant — sourcing strategy, Boolean strings for LinkedIn and Naukri, and disciplined search methodology.",
}

export default function SamAILayout({ children }: { children: React.ReactNode }) {
  return children
}
