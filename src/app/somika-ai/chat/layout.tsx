import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'SAM Chat | Ficus Logic',
}

export default function SamChatLayout({ children }: { children: React.ReactNode }) {
  return children
}
